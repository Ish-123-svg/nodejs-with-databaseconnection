# nodejs-with-databaseconnection


In this example, the frontend HTML file contains a button that triggers a request to the Node.js server, which connects to the database and retrieves data from a table. 
